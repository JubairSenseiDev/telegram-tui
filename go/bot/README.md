# telegram-tui Go bot

Standalone Telegram Bot API service for automation and lightweight replies.

```sh
export TELEGRAM_BOT_TOKEN="123:abc"
go run .
```

Commands: `/start`, `/help`, `/ping`. Plain messages are echoed back.
